language: generic

services:
  - docker

before_install:
  - docker --version
  - docker compose version || docker-compose --version

script:
  - docker build --target build -t api-productos-build .
  - docker build -t api-productos:travis .
  - docker images | grep api-productos